export interface ISelections {
  artifacts: string[]
  battalions: string[]
  endless_spells: string[]
  spells: string[]
  traits: string[]
  units: string[]
}

export interface IAllySelections {
  units: string[]
}
