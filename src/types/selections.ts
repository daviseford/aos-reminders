export interface ISelections {
  allegiances: string[]
  artifacts: string[]
  battalions: string[]
  endless_spells: string[]
  scenery: string[]
  spells: string[]
  traits: string[]
  units: string[]
}

export interface IAllySelections {
  units: string[]
}
