import GenericBattleTraits from 'army/generic/battle_traits'
import { TEffects } from 'types/data'

const BattleTraits: TEffects[] = [GenericBattleTraits.DisruptivePresence]

export default BattleTraits
